#include<stdio.h>

int main(){
    int a,b,c;
    scanf("%d %d %d",&a,&b,&c);
    int max=a;
    int min=a;
    if (a<b&&a<c)
        min=a;
    if (b<a&&b<c) 
        min=b;
    if (c<a&&c<b)   
        min=c;
    if (a>b&&a>c)
        max=a;
    if (b>a&&b>c)
        max=b; 
    if (c>b&&c>a)
        max=c;    
    int sum = (a+b+c)/3;
    if (sum>=90){
        printf("A");
    }else if (sum>=80){
        printf("B");
    }else if (sum>=70){
        printf("C");
    }else if (sum>=60){
        printf("D");
    }
    else{
         printf("F");
    }
    printf("max: %d",max);
    printf("min: %d",min);
    
    
    
 

	/* Type or paste your code in this area */

	return 0;
}
